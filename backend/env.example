# Server Configuration
# PORT: No necesario con iisnode
# IIS escucha en puerto 443 (HTTPS) - configurado en IIS Manager → Site Bindings
# Solo necesario si ejecutas Node.js directamente (sin iisnode): PORT=9905
NODE_ENV=production

# Frontend URL (for CORS)
FRONTEND_URL=https://emcopre.fritzvzla.com

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=agentkit_form
DB_USER=postgres
DB_PASSWORD=your_password_here

# OpenAI Configuration
OPENAI_API_KEY=your_openai_api_key_here
# Optional: Cloudflare proxy URL
OPENAI_PROXY_URL=https://orange-silence-9576.chiletecnologia2.workers.dev/v1

# JWT Configuration
JWT_SECRET=your_jwt_secret_here
JWT_EXPIRES_IN=24h
